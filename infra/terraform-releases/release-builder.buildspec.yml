# Reviewed entrypoint embedded into the content-addressed CodeBuild source.
version: 0.2

env:
  shell: bash

phases:
  build:
    commands:
      - chmod 0755 scripts/aws-release-builder.sh server/scripts/*.sh server/games/*/*.sh
      - scripts/aws-release-builder.sh
