# Copy to .env beside compose.yaml. .env is gitignored; this file is not.
CF_API_KEY=change-me
RCON_PASSWORD=change-me
GRAFANA_ADMIN_PASSWORD=change-me
# Keep dashboards private by default. On AWS, reach them through SSM port forwarding.
OBSERVABILITY_BIND_ADDRESS=127.0.0.1
